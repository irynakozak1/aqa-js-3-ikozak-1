const rectangleLength = 10.25;
const rectangleWidth = 5.55;
const rectangleArea = rectangleLength * rectangleWidth;
const rectangleAreaFixed = Number(rectangleArea.toFixed(2));

console.log('Area of Rectangle:', rectangleAreaFixed);
